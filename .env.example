# 环境变量示例文件
# 复制此文件为 .env 并填写实际值

NODE_ENV=production
HOST=0.0.0.0
PORT=3000

# 数据库配置（Docker 容器连接宿主机 MySQL）
DATABASE_URL=mysql://root:your_password@host.docker.internal:3306/jojo-blog

# Redis 配置（Docker 容器连接宿主机 Redis）
REDIS_HOST=host.docker.internal
